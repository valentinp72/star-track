import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'eu.vpelloin.startrack',
  appName: 'StarTrack',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
